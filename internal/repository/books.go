package repository

// search + pagination (no detail)

// get book detail

// get books of this category (search + pagination)

// get books of a author (search + pagination)

// get books of a language (search + pagination)

// get books of this publisher (search + pagination)
